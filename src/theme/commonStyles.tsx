import colors from "./colors"

const styles = {
    center: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    centerRow: {
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: "row"
    },
    container: {
        flex: 1,
        backgroundColor: colors.grey1,
        justifyContent: 'center',
        alignItems: 'center'
    },
}

export default styles;